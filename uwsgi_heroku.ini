[uwsgi]
http-socket = :$(PORT)
chdir={{ project_name }}
env=DJANGO_SETTINGS_MODULE={{ project_name }}.settings.production
master = true
die-on-term = true
module = {{ project_name }}.wsgi
memory-report = true
